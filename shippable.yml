language: none
runtime:
  - nodePool: u16
    build:
      ci:
        - echo u16
  - nodePool: aarch64
    build:
      pre_ci_boot:
        image_name: ubuntu
        image_tag: 18.04
        pull: true
      ci:
        - echo aarch64
matrix:
  include:
    - rvm: 2.0.0
      gemfile: gemfiles/Gemfile.rails-3.0.x
      env: ISOLATED=false
env:
  - foo=bar1
  - foo=bar2
build:
  pre_ci_boot:
    image_name: drydock/u16all
    image_tag: master
    pull: true
  ci:
    - echo hii
    - echo $foo
