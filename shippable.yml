language: none
runtime:
  - nodePool: u16
    build:
      pre_ci:
        - echo pre_ci
      ci:
        - echo u16
  - nodePool: restricted_aarch64
    build:
      pre_ci_boot:
        image_name: ubuntu
        image_tag: 18.04
        pull: true

build:
  pre_ci_boot:
    image_name: ubuntu
    image_tag: 16.04
    pull: true
  ci:
    - echo hii
    - echo $foo
