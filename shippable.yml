language: none
runtime:
  - nodePool: u16
  - nodePool: restricted_aarch64
  - nodePool: windows
  
  
matrix:
  include:
    - rvm: 2.0.0
      gemfile: gemfiles/Gemfile.rails-3.0.x
      env: ISOLATED=false
env:
  - foo=bar1
  - foo=bar2
build:
  ci:
    - echo hii
    - echo $foo
