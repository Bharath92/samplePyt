language: python
runtime:
  - nodePool: restricted_aarch64
    build:
      pre_ci_boot:
        image_name: drydock/aarch64_16pytall
        image_tag: master
        pull: true
  - nodePool: u16
    build:
      ci:
        - echo u16
build:
  ci:
    - echo hii
    - echo $foo
