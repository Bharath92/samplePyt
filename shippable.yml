language: python

python:
  - 2.7 #.14
  - 3.7 #.0 

runtime:
  - nodePool: restricted_aarch64
  - nodePool: u16
    build:
      ci:
        - echo u16
matrix:
  exclude:
    - python: 3.7
      nodePool: restricted_aarch64
  include:
    - python: pypy3
      nodePool: restricted_aarch64
build:
  ci:
    - echo hii
    - echo $foo
