language: none

runtime:
  nodePool: ci-builder

depth: 3


gitConfig:
  - http.sslVerify false; echo hi;
  - core.editor emacs; echo lol;


build:
  ci:
    - git log
    - git config -l | grep "http.sslverify=false"
    - git config -l | grep "core.editor=emacs"
