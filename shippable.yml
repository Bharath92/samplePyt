language: none
runtime:
  - nodePool: u16
  - nodePool: restricted_aarch64
    build:
      pre_ci_boot:
        image_name: ubuntu
        image_tag: 18.04
        pull: true

build:
  pre_ci_boot:
    image_name: ubuntu
    image_tag: 14.04
    pull: true
    options: "-e foo=boo"
  ci:
    - echo hii
    - echo $foo
