language: none
runtime:
  - nodePool: u16
  - nodePool: restricted_aarch64
    build:
      ci:
        - echo lol
      pre_ci:

build:
  pre_ci_boot:
    image_name: ubuntu
    image_tag: 14.04
    pull: true
  pre_ci:
    - echo pre_ci
  ci:
    - echo hii
    - echo $foo
